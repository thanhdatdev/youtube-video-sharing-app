<%= form_for(@movie, url: new_movie_path, class: "share-form") do |f| %>
  <%= f.text_field  :url, autofocus: true, placeholder: 'Youtube URL' %>
  <%= f.submit "Login / Register", class: "share-movie-button" %>
<% end %>
