<%= form_for(@movie, url: movies_path, class: "share-form") do |f| %>
  <%= f.text_field  :url, autofocus: true, placeholder: 'Youtube URL' %>
  <%= f.submit "Share", class: "share-movie-button" %>
<% end %>
